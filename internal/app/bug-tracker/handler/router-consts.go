package handler

const (
	id = "/:id"

	auth     = "/auth"
	signUp   = "/sign-up"
	signIn   = "/sign-in"
	logout   = "/logout"
	refresh  = "/refresh"
	verify   = "/verify-email"
	setEmail = "/set-email"

	project      = "/project"
	create       = "/create"
	update       = "/update"
	addMember    = "/add-member"
	deleteMember = "/member"
	leave        = "/leave/:id"
	setAdmin     = "/set-admin"

	task = "/task"
)
