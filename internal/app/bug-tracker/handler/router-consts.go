package handler

const (
	auth     = "/auth"
	signUp   = "/sign-up"
	signIn   = "/sign-in"
	logout   = "/logout"
	refresh  = "/refresh"
	verify   = "/verify-email"
	setEmail = "/set-email"

	project = "/project"
	create  = "/create"
)
